// measurements_plot.rs

pub struct MeasurementsPlot {
    points: Vec<f32>,
}

pub struct Point {
    x: f32,
    y: f32,
}

impl MeasurementsPlot {
    pub fn ui(&self, ui: &mut egui::Ui) {
        //egui::ScrollArea::both();
    }
}
